services:
  caldera:
    build: .
    image: caldera:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist the cloned working tree across restarts.
      - caldera-vault:/vault
    restart: unless-stopped

volumes:
  caldera-vault:
