name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Create venv + install (avoid the externally-managed system Python)
        run: |
          uv venv --python ${{ matrix.python-version }}
          uv pip install -e '.[dev]'

      - name: Configure git identity (for git-backed source tests)
        run: |
          git config --global user.email "ci@caldera.test"
          git config --global user.name "Caldera CI"
          git config --global init.defaultBranch main

      - name: Lint (ruff)
        run: uv run --no-sync ruff check caldera tests

      - name: Test (pytest + coverage)
        run: uv run --no-sync pytest -q --cov=caldera --cov-report=term-missing --cov-report=xml

      - name: Upload coverage artifact
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: coverage.xml
          if-no-files-found: ignore

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Buildx (needed for the gha cache backend)
        uses: docker/setup-buildx-action@v3
      - name: Build image (no push) to validate the Dockerfile
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          tags: caldera:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
